
#include <stdio.h>

int main()
{
    int N,sum=0;
    printf("Enter a positive integer: ");
    scanf("%d", &N);

    sum = (long long)N * (N + 1) / 2;

    printf("Sum of first %d natural numbers = %lld\n", N, sum);

    return 0;
}
